import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { toast } from 'react-toastify'; import { useAuth } from '../context/AuthContext';
export default function Register() { const { register } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ name: '', email: '', password: '', mobile: '' }); const submit = async (event) => { event.preventDefault(); try { await register(form); toast.success('Account created'); navigate('/products'); } catch (error) { toast.error(error.response?.data?.message || 'Could not create account'); } }; return <main className="auth-page"><form className="auth-card" onSubmit={submit}><p className="eyebrow">JOIN SOLESTORE</p><h1>Make some room.</h1>{[['name','Full name','text'],['email','Email','email'],['mobile','Mobile','tel'],['password','Password','password']].map(([key,label,type]) => <label key={key}>{label}<input type={type} required={key !== 'mobile'} minLength={key === 'password' ? 8 : undefined} value={form[key]} onChange={(e) => setForm({ ...form, [key]: e.target.value })} /></label>)}<button className="btn btn-dark w-100">Create account</button><p className="form-foot">Already a member? <Link to="/login">Sign in</Link></p></form></main>; }
