import { Link } from 'react-router-dom';
import { useEffect, useState } from 'react';
import { getProducts } from '../services/productService';
import ProductGrid from '../components/ProductGrid';
import LoadingSpinner from '../components/LoadingSpinner';
import { useCart } from '../context/CartContext';
import { toast } from 'react-toastify';
export default function Home() { const [products, setProducts] = useState([]); const [loading, setLoading] = useState(true); const { addItem } = useCart(); useEffect(() => { getProducts().then((response) => setProducts(response.data)).catch(() => {}).finally(() => setLoading(false)); }, []); const add = (product) => { const variant = product.variants?.find((item) => item.stockQuantity > 0); if (!variant) return toast.error('This pair is sold out'); if (!localStorage.getItem('solestore_token')) return toast.info('Log in to add items'); addItem({ productId: product.id, size: variant.size, quantity: 1 }).then(() => toast.success('Added to your bag')).catch(() => toast.error('Could not add this pair')); }; return <main><section className="hero container"><div><p className="eyebrow">THE 2026 EVERYDAY EDIT</p><h1>Shoes for the life you actually lead.</h1><p className="hero-copy">Curated sneakers, runners, and icons that go wherever you do.</p><Link className="btn btn-dark btn-lg" to="/products">Shop the collection</Link></div><div className="hero-image"><img src="https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=1200&q=85" alt="Red sneaker" /></div></section><section className="container section-block"><div className="section-heading"><div><p className="eyebrow">START HERE</p><h2>Fresh on the floor</h2></div><Link to="/products">View all <span>↗</span></Link></div>{loading ? <LoadingSpinner /> : <ProductGrid products={products.slice(0, 4)} onAdd={add} />}</section><section className="promo-band"><div className="container"><p className="eyebrow">WALK YOUR WAY</p><h2>Find the pair that keeps up.</h2><Link to="/products" className="btn btn-light">Explore all styles</Link></div></section></main>; }
