import { useEffect,useState } from 'react'; import { getAdminOrders,updateOrderStatus } from '../../services/orderService'; import { toast } from 'react-toastify';
export default function ManageOrders(){const [orders,setOrders]=useState([]);const load=()=>getAdminOrders().then(r=>setOrders(r.data)).catch(()=>toast.error('Could not load orders'));useEffect(load,[]);const update=(id,status)=>updateOrderStatus(id,status).then(load).then(()=>toast.success('Status updated')).catch(()=>toast.error('Could not update status'));return <main className="container page admin-page"><p className="eyebrow">OPERATIONS</p><h1>Orders</h1><div className="table-wrap"><table><thead><tr><th>ID</th><th>Date</th><th>Total</th><th>Payment</th><th>Status</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>#{o.id}</td><td>{new Date(o.createdAt).toLocaleDateString()}</td><td>${o.totalAmount}</td><td>{o.paymentStatus}</td><td><select value={o.status} onChange={e=>update(o.id,e.target.value)}>{['PENDING','CONFIRMED','PROCESSING','SHIPPED','DELIVERED','CANCELLED'].map(s=><option key={s}>{s}</option>)}</select></td></tr>)}</tbody></table></div></main>}
