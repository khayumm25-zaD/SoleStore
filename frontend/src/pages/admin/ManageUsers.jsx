import { useEffect,useState } from 'react'; import api from '../../services/api'; import { toast } from 'react-toastify';
export default function ManageUsers(){const [users,setUsers]=useState([]);useEffect(()=>{api.get('/users').then(r=>setUsers(r.data)).catch(()=>toast.error('Could not load users'))},[]);return <main className="container page admin-page"><p className="eyebrow">PEOPLE</p><h1>Users</h1><div className="table-wrap"><table><thead><tr><th>Name</th><th>Email</th><th>Mobile</th><th>Role</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.mobile||'—'}</td><td>{u.roleName}</td></tr>)}</tbody></table></div></main>}
