import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { getDashboard } from '../../services/orderService'; import LoadingSpinner from '../../components/LoadingSpinner';
export default function AdminDashboard(){const [data,setData]=useState(null);useEffect(()=>{getDashboard().then(r=>setData(r.data)).catch(()=>{})},[]);return <main className="container page admin-page"><p className="eyebrow">OPERATIONS</p><h1>Dashboard</h1>{!data?<LoadingSpinner/>:<div className="stat-grid">{[['Users',data.totalUsers],['Products',data.totalProducts],['Categories',data.totalCategories],['Orders',data.totalOrders],['Revenue',`$${data.totalRevenue}`],['Low stock',data.lowStockProducts]].map(([label,value])=><div className="stat-card" key={label}><small>{label}</small><strong>{value}</strong></div>)}</div>}<div className="admin-links"><Link to="/admin/products">Manage products ↗</Link><Link to="/admin/categories">Manage categories ↗</Link><Link to="/admin/orders">Manage orders ↗</Link><Link to="/admin/users">Manage users ↗</Link></div></main>}
