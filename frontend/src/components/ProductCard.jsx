import { Link } from 'react-router-dom';
import { FiArrowUpRight } from 'react-icons/fi';
export default function ProductCard({ product, onAdd }) { const stock = product.variants?.reduce((sum, variant) => sum + variant.stockQuantity, 0) || 0; return <article className="product-card"><Link to={`/products/${product.id}`} className="product-image"><img src={product.imageUrl || 'https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=800&q=80'} alt={product.name} /><span>{product.active ? 'Available' : 'Unavailable'}</span></Link><div className="product-meta"><small>{product.brand}</small><Link to={`/products/${product.id}`}><h3>{product.name}</h3></Link><div className="d-flex justify-content-between align-items-center"><strong>${product.price}</strong><span className="stock-label">{stock ? `${stock} in stock` : 'Sold out'}</span></div><button className="btn btn-dark w-100 mt-3" disabled={!product.active || !stock} onClick={() => onAdd(product)}>Quick add <FiArrowUpRight /></button></div></article>; }
