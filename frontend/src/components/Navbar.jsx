import { Link, NavLink, useNavigate } from 'react-router-dom';
import { FiLogOut, FiShoppingBag } from 'react-icons/fi';
import { useAuth } from '../context/AuthContext';
import { useCart } from '../context/CartContext';
export default function Navbar() { const { user, logout, isAdmin } = useAuth(); const { count } = useCart(); const navigate = useNavigate(); return <nav className="navbar navbar-expand-lg site-nav"><div className="container"><Link className="navbar-brand" to="/">SOLE<span>STORE</span></Link><button className="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#nav"><span className="navbar-toggler-icon" /></button><div id="nav" className="collapse navbar-collapse"><div className="navbar-nav me-auto"><NavLink className="nav-link" to="/products">Shop</NavLink>{isAdmin && <NavLink className="nav-link" to="/admin">Admin</NavLink>}</div><div className="d-flex align-items-center gap-3"><Link className="bag-link" to="/cart"><FiShoppingBag /> <small>{count}</small></Link>{user ? <><Link className="nav-user" to="/profile">{user.name}</Link><button className="btn btn-link nav-logout" onClick={() => { logout(); navigate('/'); }} title="Log out"><FiLogOut /></button></> : <><Link className="btn btn-outline-dark btn-sm" to="/login">Log in</Link><Link className="btn btn-dark btn-sm" to="/register">Join</Link></>}</div></div></div></nav>; }
