import { FiMinus, FiPlus, FiTrash2 } from 'react-icons/fi';
export default function CartItem({ item, onUpdate, onRemove }) { return <div className="cart-item"><img src={item.imageUrl || 'https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=300&q=80'} alt={item.productName} /><div className="flex-grow-1"><small>Size {item.size}</small><h5>{item.productName}</h5><span>${item.price}</span></div><div className="quantity-control"><button onClick={() => onUpdate(Math.max(1, item.quantity - 1))} title="Decrease"><FiMinus /></button><b>{item.quantity}</b><button onClick={() => onUpdate(item.quantity + 1)} title="Increase"><FiPlus /></button></div><strong>${item.subtotal}</strong><button className="icon-button" onClick={onRemove} title="Remove"><FiTrash2 /></button></div>; }
